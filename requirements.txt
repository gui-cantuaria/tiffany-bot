# Version floors keep installs reproducible while allowing security patches.
# For an EXACT lock of the currently-working VPS, run there:
#     pip3 freeze > requirements.lock
# and deploy from that when you need byte-for-byte reproducibility.
#
# NOTE: yt-dlp is intentionally left UNPINNED — YouTube changes often and old
# yt-dlp versions stop working. Keep it fresh (pip install -U yt-dlp).

discord.py[voice]>=2.5.0,<3
discord-ext-voice-recv>=0.5.2a179
wavelink>=3.4.0,<4          # code targets the wavelink 3.x API (Pool, Playable.search)
feedparser>=6.0
python-dotenv>=1.0
aiohttp>=3.9
openai>=1.30                # requires the v1+ client (openai.AsyncOpenAI)
yt-dlp                      # keep unpinned — see note above
SpeechRecognition>=3.10
edge-tts>=6.1
gTTS>=2.4
vosk>=0.3.44
beautifulsoup4>=4.12
